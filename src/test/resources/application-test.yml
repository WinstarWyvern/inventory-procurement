spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/inventory_procurement_test
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    clean-disabled: false

app:
  jwt:
    secret: test-secret-test-secret-test-secret-test-secret
    expiration-minutes: 480
